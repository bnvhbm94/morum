# Fable 지휘자 브리프 — Morum 시스템/문서모델/UI 개선

이 문서 전체를 Claude Code의 Fable 세션에 그대로 보낸다.
작성: 2026-09-22. 작성자: 선행 Opus 세션(측정값은 이 세션이 실제로 실행해 얻은 것).

---

## 0. 너의 역할

너는 **총괄 지휘자**다. 직접 타이핑하는 사람이 아니라, 읽고 · 판단하고 · 쪼개고 · 위임하고 · **모든 diff를 직접 읽어 검증하는** 사람이다.

작업 루프는 이 순서를 지킨다: **조사 → 판단 → 계획 → 위임 → diff 리뷰 → 실브라우저 검증 → 보고**.

이 브리프에서 가장 중요한 산출물은 코드가 아니라 **판단**이다. 특히 §3의 두 질문(포크 모델이 옳은가, 원 구상을 어떻게 보는가)에 대한 너의 솔직한 의견이 코드보다 우선한다. 동의하지 않으면 동의하지 않는다고 써라. 사용자는 아첨이 아니라 판단을 원한다.

최종 보고는 **한국어**로 한다.

---

## 1. 먼저 읽을 것 (전부, 빠짐없이)

### 1-1. 원본 의도 — 이게 진실의 기준이다
- `MY THOUGHT/1.md`, `2.md`, `3.md`, `4.md` (한국어, 큼)
  - 사용자가 직접 쓴 원 구상과, 그에 대한 AI 정리·조사다.
  - `3.md`에 저장소 옛 이름이 `nuanox`였다고 나온다. **역사적 맥락일 뿐 실행 대상이 아니다.**
  - `4.md`는 관련 논문·표준 목록이다. 특히 Micropublications, Nanopublications, ATMS(de Kleer), Truth Maintenance System(Doyle), Dung의 논증 프레임워크, PROV-O, W3C Web Annotation은 §3-1 판단에 직접 쓰인다.

### 1-2. 프로젝트 규칙
- `AGENTS.md` — **이 저장소는 커스터마이즈된 Next.js 16.3.5다. 네 학습 데이터와 API·관례가 다를 수 있다.** 라우팅/데이터페칭을 건드리기 전에 `node_modules/next/dist/docs/` 의 해당 가이드를 읽어라.

### 1-3. 누적된 인수인계
- `docs/PROJECT_BRIEF.md`, `docs/CODE_MAP.md`
- `docs/SPATIAL_UI_HANDOFF.md` ← 방금 갱신됨. **§포인터 캡처 문단은 함정 재발 방지용이니 반드시 읽어라.**
- `docs/SPATIAL_DATA_HANDOFF.md`, `docs/VISUAL_QA_HANDOFF.md`
- `TEMP_AGENT_HANDOFF.md`, `MORUM_AGENT_PLAN.md`

### 1-4. 코드
- UI: `src/components/spatial-explorer.tsx`, `src/app/globals.css`, `src/app/page.tsx`
- 데이터 어댑터: `src/lib/spatial-data.ts`, `src/lib/api-client.ts`
- 서버: `src/server/service/routes.ts`, `http.ts`, `factory.ts`, `retrieval.ts`, `auth.ts`
- 계약: `src/contracts/types.ts`
- 저장소/스키마: `src/server/db/knowledge-repository.ts`, `supabase/migrations/`
- 테스트: `tests/static/`, `tests/service/`, `tests/server/`

> 이 문서들은 불변이 아니다. **사용자의 최신 구두 지시가 문서와 충돌하면 사용자가 이긴다.** 다만 "원래 의도에 더 가깝게"가 무슨 뜻인지 판단하려면 원본을 먼저 이해해야 한다.

---

## 2. 이미 측정된 사실 (다시 파헤치지 말고 여기서 출발해라)

아래는 선행 세션이 **실제로 실행해 확인한** 값이다. 추정이 아니다.

### 2-1. 성능 — 사용자의 "로드가 너무 오래 걸린다"는 사실이다
운영(`https://morum.vercel.app`) 실측, 2026-09-22:

| 엔드포인트 | total | 비고 |
|---|---|---|
| `GET /api/v2/records?limit=20` | **1.39 / 1.87 / 2.24 / 2.39 / 1.39 초** (5회) | 문서 5건뿐인데 이 정도 |
| `POST /api/v2/search` | **2.23 / 2.08 초** | |

여기에 `SEARCH_DELAY = 1000ms` 디바운스가 더해져 **검색 체감이 3초 이상**이다.
원인 후보(네가 확인할 것): Vercel 함수 콜드스타트, Supabase 리전(프로젝트 sandbox 리전 `iad1`)과의 왕복, RPC 쿼리 자체, 임베딩 경로, 커넥션 재사용 여부.

### 2-2. "체인지가 이상하게 보인다" — 원인 2개
1. `loadPage()`가 결과를 받을 때마다 `setSelected(0)` + `cameraRef.current = {x:0,y:0}`로 **카메라를 원점으로 순간이동**시킨다. 전환 애니메이션도, 로딩 스켈레톤도 없다. 1.4~2.4초 빈 화면 뒤 갑자기 튀어나온다.
2. ~~**관성 속도에 상한이 없다.**~~ → **2026-09-22 수정 완료. 다시 보고하지 마라.** `finishPointer`의 `pointer.vx = dx/dt*16`은 `dt`가 작으면 속도가 폭발했다(실측: 230px 드래그가 카메라를 **1887px** 이동). 이제 `MAX_FLICK = 60`(px/frame)으로 속도 벡터 크기를 방향 보존하며 클램프한다. 같은 드래그가 723px로 줄었다. 감쇠 계수 `.92`는 그대로다.
   - **다만 남은 것**: `pointer.vx`는 여전히 **마지막 move 델타 하나**로만 계산된다. 이동 평균이나 최근 N개 샘플로 바꾸면 관성이 더 자연스러워진다. 이건 네 판단 영역이다.

### 2-3. ~~텍스트 선택 버그~~ → 2026-09-22 수정 완료. 다시 보고하지 마라.
`.spatial-node`가 `user-select: auto`여서 노드 본문 위를 드래그하면 텍스트가 선택됐다(사용자 스크린샷에 찍혀 있었다). `.spatial-node`에 `-webkit-user-select: none; user-select: none`을 추가했다. 더블클릭 선택을 막던 `onMouseDown`의 `detail>=2` 처리는 그대로 두었다(중복이지만 무해하고, CSS가 빠져도 방어된다).

### 2-4. 바둑판 배치의 근본 문제 — 이게 사용자가 "바둑판이 괜찮을지 모르겠다"고 한 것의 정체다
`spatialCoordinate(index)`는 **배열 인덱스를 나선으로 펼치는 순수 함수**다. 좌표가 관계에 대해 **아무 정보도 담고 있지 않다.**

즉 지금 화면은 "관계 있는 문서들이 가까이 있는 2D 공간"이 아니라 **검색/최신순 리스트를 나선으로 접어 놓은 것**이다. 사용자의 전제(`MY THOUGHT/1.md`의 "여러 문서가 뭉쳐진 덩어리 정보", `2.md` §6)와 정면으로 어긋난다. 게다가 `docs/SPATIAL_DATA_HANDOFF.md`는 "근접성을 논리적 관계로 착각하지 말 것"을 명시하는데, 현재 배치는 **근접성이 아무 의미도 없으면서 의미가 있는 것처럼 보이게** 만든다. 이건 단순한 미감 문제가 아니라 **오해를 유발하는 설계**다.

### 2-5. 폰트가 실제로 로드되지 않음
Pretendard / Source Serif 4 / Noto Serif KR 모두 **웹폰트로 로드되지 않는다**(측정으로 확인). 개발 Mac에서는 Apple SD Gothic Neo·명조 계열로 폴백돼 그럴듯해 보이지만, Windows·Linux·모바일에서는 의도한 타이포가 보장되지 않는다.

### 2-6. 검색창 디자인
방금 liquid glass로 교체됐다(약한 내부 틴트 + `blur/saturate/brightness` + inset 하이라이트/림 + 블러 의사요소). 사용자는 여전히 "조잡하다"고 느낀다. **재질이 아니라 배치·비례·정보구조의 문제일 수 있다**는 가능성을 먼저 검토해라. 하단 중앙 고정 알약 하나가 이 제품의 유일한 진입점으로 적절한가?

---

## 3. 네가 답해야 할 핵심 질문 (코드보다 우선)

### 3-1. 포크(버전 분기) 모델이 에이전트에게 좋은가? 유지보수에 괜찮은가?

현재 구조: 원문 보존 + 수정은 새 버전/분기, 관계(`supports`/`corrects`/`depends_on`/`derived_from`/…), 검토(동의/반대/추가검토), 기본 표시는 **최신 버전**.

최소한 이걸 판단해라:
- 에이전트가 `/records`를 읽을 때 **"최신"이 옳은 기본값인가?** `MY THOUGHT/2.md` §7은 "최신 ≠ 가장 신뢰할 만함"을 명시했고, 프로토타입 편의로 최신을 택했다. 지금도 유효한가?
- **같은 버전에서 10개 에이전트가 각자 포크하면** 읽는 쪽은 무엇을 근거로 고르나? 분기 집합이 탐색 불가능해지는 지점은 어디인가? 병합(merge) 이야기가 아예 없는데 필요한가, 아니면 Dung식으로 "공존하는 입장"으로 두는 게 맞나?
- **정정의 전파**: A가 틀렸다고 판정되면 A를 근거로 한 B·C를 재검토 대상으로 찾을 수 있나? (`4.md`의 ATMS / Truth Maintenance System)
- Micropublications / Nanopublications와 비교했을 때 현재 모델이 더 나은 점과 나쁜 점은?
- **유지보수 비용**: 마이그레이션 부담, RPC 표면적, 커서/페이지네이션, 스키마가 굳어지는 지점.

결론은 셋 중 하나로 분명히 내라: **(a) 유지**, **(b) 유지하되 이러이러하게 보강**, **(c) 다른 모델로 교체**. 각각의 마이그레이션 비용과 되돌릴 수 없는 지점을 같이 적어라.

### 3-2. 사용자의 원 구상을 너는 어떻게 보는가

`MY THOUGHT/*.md`를 다 읽고 솔직하게 써라. 칭찬 목록 말고, **어디가 강하고 어디가 약하며 무엇이 아직 검증되지 않은 가설인지**. 사용자 본인도 반복해서 "이게 최선인지 모르겠다"고 적어놨다. 그 불확실성에 실제로 답해라.

특히 다뤄라:
- 의미 참조(`배`→`선박`)가 UI에 없다. 사용자가 `3.md`에서 가장 아쉬워한 부분이다. 지금 넣을 값어치가 있나, 아니면 저장소가 더 쌓인 뒤인가?
- "덩어리 정보"(여러 문서가 뭉친 인식)와 현재 `/context` 구현 사이의 간극.
- 보상/암호화폐는 사용자가 후순위로 정했다. 그 판단이 옳은가?

---

## 4. 개선 범위 (우선순위는 네가 판단해 제안하고, 사용자 승인 후 실행)

사용자가 직접 언급한 불만:
1. 문서 관리/시스템 측면 — §3-1
2. 사이트 디자인이 부실하다
3. 글과 글의 관계를 나타내는 데 바둑판 형식이 맞는지 모르겠다 — §2-4
4. 하단 검색창이 조잡하다 — §2-6
5. 로드가 너무 오래 걸린다 — §2-1
6. 로드/전환이 이상하게 보인다 — §2-2
7. 앞으로의 유지보수

여기에 §2-3(텍스트 선택), §2-5(폰트)를 더해라.

**한 번에 다 하지 마라.** 단계로 쪼개고, 각 단계마다 사용자 승인을 받아라.

---

## 5. 에이전트 운용 규칙 (서브에이전트 · 병렬 세션)

### 5-1. 기본은 서브에이전트다 — 이 프로젝트에서는 결론이 났다

"별도 Claude Code 세션을 여러 개 띄우는 것(병렬 세션)"과 "한 세션 안에서 서브에이전트를 쓰는 것" 중, **이 저장소에서는 서브에이전트가 기본이다.** 이유는 취향이 아니라 이 프로젝트의 구체적 조건이다:

1. **읽어야 할 원본이 크고 공유된다.** `MY THOUGHT/*.md`가 합쳐 274KB다. 병렬 세션은 각 세션이 이걸 따로 전부 읽어야 한다. 서브에이전트는 네가 한 번 읽고 **요약해서** 넘기면 된다. Max 5x 윈도우에서 이 차이는 크다.
2. **hot file이 겹친다.** 변경이 `src/components/spatial-explorer.tsx`와 `src/app/globals.css` 두 개에 집중된다. 병렬 세션 + worktree는 여기서 머지 충돌을 만든다. 서브에이전트는 한 워킹 트리에서 **파일 소유권만 분리**하면 충돌이 0이다(선행 세션에서 실측 확인).
3. **검증 환경이 하나뿐이다.** 브라우저 창 1개, 포트 3000 dev 서버 1개, Supabase 프로젝트 1개. 선행 세션은 실제로 **포트 3000이 다른 세션에 점유돼 막혔다.** 세션을 늘리면 이 충돌이 비례해 늘어난다.
4. **조율 부담이 사용자에게 간다.** 병렬 세션은 기본적으로 지휘자가 없다. 사용자는 세션 여러 개를 동시에 지켜보고 싶어하지 않는다. 그래서 너를 총괄로 세운 것이다.
5. **1단계 작업이 병렬화가 안 된다.** §3의 두 질문은 **하나의 일관된 판단**이다. 쪼개서 여럿에게 시킬 성질이 아니다.

참고: `agent teams`(세션 자동 조율)는 실험 기능이고 기본 비활성이다. 이 작업의 토대로 삼지 마라.

### 5-2. 병렬 세션을 쓰는 단 하나의 경우 — 백엔드 성능 조사

§2-1(API 1.4~2.4초)은 **별도 세션으로 빼라.** 조건이 전부 맞는 유일한 작업이다:

- `src/server/`, `supabase/migrations/`를 건드려 **UI 작업과 파일이 겹치지 않는다**
- 쿼리 플랜·인덱스·콜드스타트를 **반복 측정하며 깊은 맥락을 쌓아야** 하는데, 그걸 네 컨텍스트에 쌓으면 §3 판단이 밀린다
- 사용자가 중간에 끼어들어 방향을 틀 만한 종류의 일이다

**병렬 세션 분리 판단 기준** (셋 다 충족할 때만):
1. 다른 작업과 편집 파일이 **완전히 분리**되는가
2. 맥락이 **깊게 쌓이고 재사용되지 않는가**(= 요약해서 넘기면 손실이 큰가)
3. 사용자가 **직접 지켜보고 개입하고 싶어할** 일인가

하나라도 아니면 서브에이전트로 해라.

**세션 간 인계 방법**: 별도 세션의 결과는 대화 전문이 아니라 **결론과 수치만** 넘겨받아라. 넘겨받을 것은 (a) 측정 전후 수치, (b) 원인 진단, (c) 변경한 파일 목록, (d) 남은 위험. 그 세션이 커밋하지 않도록 하고, 최종 커밋 판단은 너와 사용자가 한다.

### 5-3. 단계별 권장 형태

| 단계 | 방식 |
|---|---|
| 1. 판단 (포크 모델 · 원 구상 평가) | **너 단독.** 코드 사실 확인용 Explore 서브에이전트 1~2개만 |
| 2. 계획 · 승인 | **너 + 사용자** |
| 3. UI 구현 | **너 + Sonnet 서브에이전트 2~3개**, 파일 소유권 분리 |
| 3-b. 백엔드 성능 | **별도 병렬 세션** (§5-2) |
| 4. 리뷰 | fresh-context 리뷰 **서브에이전트** 1개 |

3과 3-b는 동시에 돌려도 되지만, **합쳐서 동시 3개를 넘기지 마라**(§5-4 예산).

### 5-3-a. 위임할 일과 직접 할 일 (공식 가이드 기준)
- **위임한다**: 독립적인 작업 3개 이상 / 파일 10개 이상 탐색 / fresh context 검증이 필요할 때.
- **직접 한다**: 순차 의존 작업, 같은 파일 편집, 작고 빠른 수정, 긴밀한 조율이 필요한 단계.
- 탐색 한 번이면 끝날 일에 서브에이전트를 띄우지 마라. 그게 가장 흔한 낭비다.

### 5-4. 예산 — Max 5x 기준 (중요)
공식 문서는 "Pro의 5배"라는 배수만 밝히고 정확한 수치를 공개하지 않는다. 커뮤니티 실측 추정으로 **5시간 롤링 윈도우당 Claude Code 프롬프트 약 50~200개**이며, **병렬 서브에이전트를 쓰면 1시간~1시간 15분 만에 윈도우를 소진**할 수 있다. 따라서:

- **동시 실행 에이전트는 서브에이전트와 병렬 세션을 합쳐 3개를 넘기지 마라.** 병렬 세션은 서브에이전트보다 비싸다(맥락을 요약받지 못하고 원본을 직접 읽는다). §5-2의 백엔드 세션을 돌리는 동안에는 서브에이전트를 2개까지만 써라.
- **구현은 Sonnet에 위임**하고(`model: "sonnet"`), Fable(너)은 읽기·판단·리뷰·검증에 써라.
- 단계를 **순차로** 진행해라. 1단계 승인 전에 2단계 에이전트를 띄우지 마라.
- 한 서브에이전트에 **파일 하나~소수**만 주고 범위를 좁혀라. 범위가 넓을수록 토큰이 기하급수로 샌다.
- 윈도우가 소진될 것 같으면 **중간 상태를 문서로 남기고 멈춰라.** 반쯤 고친 코드를 남기고 끊기는 게 최악이다.

### 5-5. 서브에이전트 프롬프트 작성법 (사용자가 "아주 자세히" 요구한 부분)
서브에이전트는 **이 대화의 맥락을 하나도 상속받지 않는다.** 매번 아래를 전부 넣어라:

```
1. 저장소 절대경로, 스택(Next.js 16.3.5 / React 19 / TS), AGENTS.md를 먼저 읽으라는 지시
2. 편집 허용 파일 목록 — "이 파일만. 다른 파일은 손대지 마라"
   (다른 서브에이전트가 동시에 만지는 파일이 있으면 명시적으로 금지)
3. 지금 코드가 어떻게 생겼는지 (관련 부분 그대로 붙여넣기)
4. 왜 이게 문제인지 — 근거/측정값 포함
5. "done"의 정의 — 관찰 가능한 조건으로
6. 지켜야 할 제약 (아래 §6 금지사항 전부 + 정적 테스트가 깨는 문자열)
7. 실행할 검사: npm run typecheck / npm run test:static / npm run build
8. 보고 형식: 실제 diff + 검사 출력 그대로 + 불확실했던 점
9. "커밋하지 마라. dev 서버 띄우지 마라. .env 값을 읽거나 출력하지 마라."
```

### 5-6. 검증
- **서브에이전트의 요약을 믿지 마라. 네가 직접 `git diff`를 읽어라.** 선행 세션에서 서브에이전트 요약은 정확했지만, 그걸 확인한 건 diff를 직접 읽었기 때문이다.
- 실브라우저 검증은 **네가** 해라. 서브에이전트에게 맡기지 마라(브라우저 창은 하나다). 같은 이유로 **병렬 세션에도 브라우저·dev 서버를 동시에 쓰게 하지 마라.** §5-2의 백엔드 세션은 `curl`로 API를 직접 때려 측정하게 하고, 브라우저는 네가 독점해라.
- 병렬 세션의 결과도 요약만 믿지 말고 **네가 `git diff`를 읽어라.** 인계받을 것은 §5-2에 적힌 네 가지(수치·진단·파일·위험)뿐이다.
- 구현이 끝나면 **adversarial review 서브에이전트**를 fresh context로 띄워 diff를 검토시켜라. 단, 프롬프트에 **"correctness와 명시된 요구사항 누락만 보고하고 스타일 취향은 보고하지 마라"**를 반드시 넣어라. 안 넣으면 없는 문제를 만들어내서 과잉 엔지니어링으로 끌고 간다.
- 각 서브에이전트에게 **스스로 돌릴 수 있는 검사**를 줘라. 없으면 네가(그리고 사용자가) 검증 루프가 된다.

---

## 6. 절대 하면 안 되는 것 (Fable도, 서브에이전트도)

### 6-1. 배포·git
- 사용자의 **채팅상 명시적 승인 없이** commit / push / 배포 금지. 한 번의 승인은 그 한 번에만 유효하다.
- `vercel --prod`, `vercel alias set` 등 **운영 반영 금지**. (참고: 이 프로젝트는 원격 git이 없어 `vercel` CLI 직접 배포만 가능하고, `--prod`만으로는 `morum.vercel.app` alias가 안 옮겨간다. 그래도 승인 없이 하지 마라.)
- `git reset --hard`, `git push --force`, 브랜치 삭제 등 파괴적 git 금지.
- **기존 미커밋 변경을 되돌리거나 덮어쓰지 마라.**
- 서브에이전트에게 commit 권한을 주지 마라.

### 6-2. 비밀값
- `.env.local`, `.env`, Supabase/Vercel 시크릿을 **읽거나 출력하거나 하드코딩하지 마라.** 필요하면 **존재 여부만** 확인해라(`[ -n "$VAR" ]`). 값은 절대 출력 금지.
- 서브에이전트에게도 같은 제약을 명시적으로 걸어라.

### 6-3. 호환성을 깨는 변경
- `src/server/service/auth.ts`의 credential prefix `nuanox_`와 HMAC domain-separation 문자열 `nuanox-agent-v1\0`을 **바꾸지 마라.** 기존 credential이 전부 인증 불가가 된다. DB에 digest가 이미 존재한다. 이름 변경은 `TEMP_AGENT_HANDOFF.md`의 단계적 호환 계획을 따라야 하고, 그건 별도 승인 사항이다.
- DB 마이그레이션·스키마 변경은 **명시적 승인 + 롤백 절차 제시** 없이 금지.
- 공개 API(`/api/v2/*`) 계약을 조용히 바꾸지 마라. 외부 에이전트가 쓰는 표면이다.

### 6-4. 제품 원칙 (사용자가 반복해서 요구한 것)
- **목업/데모 문서 배열 금지.** 실제 서버 데이터만. (`tests/static/spatial-ui.test.mjs`가 `mockDocuments|demoDocuments|const documents = [`를 막고 있다.)
- 탐색 화면에 **선·화살표·카드 테두리·격자선 금지.** 관계의 방향성은 배치로만 표현한다.
- 탐색 지도에 **WebGL / Three.js / FluidGlass / SpecularButton / SVG displacement / 큰 blur 레이어 금지.** `backdrop-filter`는 작은 크롬(검색창·닫기 버튼)에만.
- `globals.css`에 **`gradient` 문자열 금지** — `tests/static/stage05-ui.test.mjs`가 `gradient|text-align: justify|position: sticky`를 차단한다. 이건 "AI 그라데이션 슬롭" 방지용 가드다.
- 탐색 화면에 "모름/메뉴" 같은 문구나 StaggeredMenu류 메뉴를 **다시 넣지 마라.** 이미 거부된 방향이다.
- 검색창에 홈 버튼을 만들지 마라. **빈 검색어 자체가 홈이다.**

### 6-5. 최근에 고친 함정을 되돌리지 마라
- `setPointerCapture`를 **`onPointerDown`으로 되돌리지 마라.** 그렇게 하면 포인터 캡처가 `pointerup`/`mouseup`/`click`을 `.spatial-viewport`로 재지정해서 노드 버튼의 `onClick`이 아예 실행되지 않는다. 클릭으로 문서를 열 수 없게 된다. 지금은 `DRAG_THRESHOLD`를 넘는 첫 `pointermove`에서만 캡처한다. 자세한 건 `docs/SPATIAL_UI_HANDOFF.md`에 적혀 있다.
- 단일 클릭이 리더를 여는 동작으로 되돌리지 마라. **단일 클릭 = 선택+센터링, 빠른 더블클릭/더블탭 = 열기**가 현재 확정된 사양이다.
- 정적 테스트가 존재를 검사하는 문자열을 지우지 마라: `DRAG_THRESHOLD`, `suppressClickRef`, `onPointerCancel`, `setPointerCapture`, `generation !== generationRef.current`, `pageRequestRef.current?.abort`, `event.isComposing`, `worldRef.current.style.transform = \`translate3d`, `.spatial-world`의 `will-change: transform`, `@media (max-width: 850px)`, `@media (prefers-reduced-motion: reduce)`.

### 6-6. 정직성
- **하지 않은 검증을 했다고 쓰지 마라.** 테스트가 실패하면 출력 그대로 보고해라. 건너뛴 단계는 건너뛰었다고 말해라.
- 데이터를 지어내지 마라. 로컬 `.env.local`이 동작하고 Supabase에 실제 문서가 있다. 그걸 써라.
- 서브에이전트가 틀린 보고를 할 수 있다. 액면 그대로 믿지 마라.

### 6-7. 범위
- 이건 **개선 패스지 재작성이 아니다.** 프레임워크 교체, 상태관리 라이브러리 도입, 디자인 시스템 전면 도입은 **제안만 하고 실행하지 마라.**
- 새 런타임 의존성 추가는 승인 사항이다. 현재 dependencies는 `next`, `react`, `react-dom`, `ogl`, `server-only`뿐이다. 이 절제를 존중해라.
- `MY THOUGHT/`, `TEMP_AGENT_HANDOFF.md`, `MORUM_AGENT_PLAN.md`, `docs/*`를 지우지 마라.
- `AGENTS.md`의 `nextjs-agent-rules` 블록을 지우지 마라. `next dev`가 다시 만든다.

### 6-8. 환경
- **포트 3000에 dev 서버를 무작정 띄우지 마라.** 다른 세션이 쓰고 있을 수 있다. 먼저 확인하고, 남의 서버를 죽이지 마라.
- 서브에이전트에게 dev 서버를 띄우게 하지 마라.

---

## 7. 검증 게이트 (전부 통과해야 "됐다"고 말할 수 있다)

- `npm run typecheck`
- `npm run test:static` (현재 18/18)
- `npm run build`
- **실브라우저 + 실제 Supabase 데이터**로: 단일 클릭이 리더를 열지 않음 / 더블클릭이 엶 / 드래그 후 관성 유지하며 리더 안 열림 / 화살표+Enter / 모바일 뷰포트 / 콘솔 에러 없음
- 성능 개선을 했다면 **개선 전후 수치**를 같이 제시해라(§2-1이 기준선이다).

---

## 8. 산출물

1. **판단 문서** — §3-1(포크 모델)과 §3-2(원 구상 평가)에 대한 너의 결론. 근거 포함. 코드보다 먼저.
2. **우선순위 계획** — 단계별로, 각 단계의 비용/위험/되돌릴 수 있는지, 그리고 **명시적인 범위 제외 목록**.
3. **구현** — 승인된 단계만, 리뷰된 청크 단위로.
4. **검증 보고** — 주장 말고 증거(명령어와 출력, 스크린샷, 수치).
5. 필요하면 `docs/` 핸드오프 갱신.

---

## 9. 시작 방법

먼저 **plan mode로 §1을 전부 읽고 §3의 두 질문에 답하는 것부터 해라.** 코드를 건드리기 전에 그 판단을 사용자에게 제시하고 승인을 받아라.

읽는 중에 범위나 접근에 대해 갈리는 지점이 나오면, 혼자 정하지 말고 `AskUserQuestion`으로 사용자에게 물어라. 다만 답이 없어도 진행할 수 있는 일은 먼저 진행해라.
