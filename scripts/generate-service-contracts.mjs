/** Both distributions are generated from the executable route table, not a second inventory. */
import {writeFile,mkdir} from 'node:fs/promises';import {ROUTES} from '../.test-build/server/service/routes.js';import {CONTRACT_VERSION} from '../.test-build/contracts/types.js';
const body={contract_version:CONTRACT_VERSION,base_path:'/api/v2',entry:'/skill.md',stage:'04-open-contribution-runtime-blocked',contribution_mode:'anonymous_without_registration',body_input:'text/plain or JSON body_text; plain_text default; Markdown optional',legacy_v1:'retired; no compatibility forwarding',routes:ROUTES.map(([method,path,auth,response])=>({method,path:'/api/v2'+path,auth,response,registration_required:false,optional_legacy:auth==='agent'||auth==='enrollment_credential',operator_only:auth==='operator',implementation:'app/src/server/service/http.ts',verification:'see reports/stage04_revision/TEST_RESULTS.json; route inventory is not runtime proof'}))};
const text=JSON.stringify(body,null,2)+'\n';await mkdir('public/agent',{recursive:true});await writeFile('public/agent/api-routes.json',text);await writeFile('../contracts/API_ROUTES.json',text);
console.log(JSON.stringify({contract_version:CONTRACT_VERSION,routes:ROUTES.length,outputs:['public/agent/api-routes.json','../contracts/API_ROUTES.json']}));
